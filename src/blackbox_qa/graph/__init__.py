"""LangGraph multi-agent variant of the agent (opt-in, keeps the framework-free
`agent.py` as the untouched baseline).

Install the extra deps first:  poetry install --with langgraph
Set a free OpenAI-compatible endpoint in .env (Groq / Gemini / Ollama), same as
the baseline agent uses.
"""
